def count_segments(n: int, m: int, segments: list, queries: list) -> tuple:
    """
    Parameters:
    :n (int): number of segments
    :m (int): number of queries
    :segments (list): list of segment's boundaries
    :queries (list): list of queries
    :return: tuple of query answers
    """
    pass
